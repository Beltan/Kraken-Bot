api = require('api_wrapper');
ia = require('ia');


// main
while(true){
    // api.inicialze(simualtion = true, moneda = )

    // values = api.getValues()

    // decision = ia.decide(values)

    // api.makeDecision(decision)
}